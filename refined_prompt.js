// Refined prompt
// Write a JavaScript function named camelCase that converts a given string to camelCase.
// Requirements:
// - Handle strings with spaces, hyphens, or underscores
// - Ignore special characters
// - Handle numbers correctly
// - Throw a descriptive error if input is not a string
// Examples:
// camelCase("Hello World") -> "helloWorld"
// camelCase("user_id") -> "userId"
